groups:
  admin:
    - limboserver.stop
    - limboserver.kick
    - limboserver.say
    - limboserver.gamemode
    - limboserver.transfer
  default:
    - limboserver.spawn
    - limboserver.chat
    - limboserver.version

players:
  LOOHP: 
    - admin