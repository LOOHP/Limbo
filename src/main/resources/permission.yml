groups:
  admin:
    - limboserver.stop
    - limboserver.kick
    - limboserver.say
    - limboserver.gamemode
  default:
    - limboserver.spawn
    - limboserver.chat

players:
  LOOHP: 
    - admin